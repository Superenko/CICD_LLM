import type { To } from 'react-router';

export const ANIMATION_DURATION = 300;

export const SEARCH_QUERY_PARAM = 'q';

export const MAX_SELECTED_VALUES = 10;

export const NAV_LINKS: { label: string; to: To }[] = [
  {
    label: 'Dashboard',
    to: '/'
  }
];
